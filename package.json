{
  "name": "neural-network",
  "version": "1.0.0",
  "description": "Library that create neural networks",
  "main": "index.js",
  "author": "Yegor Knyazev (@yegorweb)",
  "license": "MIT",
  "scripts": {
    "start": "npm run build:live",
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^18.13.0",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "tslib": "^2.5.0",
    "typescript": "^4.9.5"
  }
}
