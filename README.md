# Библиотека для создания нейросетей прямого распространения для задач классификации

## Before using:
```
sudo npm install -g typescript
sudo npm install -g @types/node
sudo npm install -g ts-node
npm i
```

## Start:
```
npm run start
```